{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <div class="container">
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ category.naam }}</td>
            </tr>
        </tbody>
    </table>

    <span><a class="btn btn-outline-info" href="{{ path('category_index') }}">back to list</a></span>

    <span><a class="btn btn-outline-dark" href="{{ path('category_edit', {'id': category.id}) }}">edit</a></span>

    <span {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
