{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="mt-5">Edit Category</h2>

        <span {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}
        <span><a class="btn btn-outline-info" href="{{ path('category_index') }}">back to list</a></span>
        <span {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
